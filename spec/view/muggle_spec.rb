require 'spec_helper'

describe View::Muggle do
  it 'has a version number' do
    expect(View::Muggle::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
